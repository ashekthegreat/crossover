'use strict';

  beforeEach(module('myApp'));

  describe('ChangelistController', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ChangelistController = $controller('ChangelistController');
      expect(ChangelistController).toBeDefined();

    }));

  });
